# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MongoDB)
MONGODB_URI=mongodb://localhost:27017/truco_game
# Alternative format: mongodb://username:password@host:port/database

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
# In development, localhost origins are automatically allowed
# Set FRONTEND_URL to your deployed frontend URL for production
# You can also specify multiple URLs (comma-separated) in FRONTEND_URLS
FRONTEND_URL=http://localhost:8080
# FRONTEND_URLS=https://your-frontend-domain.com,https://another-domain.com

