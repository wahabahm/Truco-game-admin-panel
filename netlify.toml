[build]
  command = "npm run build"
  publish = "dist"

# IMPORTANT: In Netlify Environment Variables, set:
# VITE_API_URL = https://truco-game-admin-panel-production.up.railway.app/api
# Make sure to include /api at the end of the URL

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
